/*Crie uma solução que verifica se o usuário escolheou a cor 'Azul'. Caso seja uma outra cor, utilize o not operator para retornar a mensagem: "Não temos essa cor*/

let cor = 'marelo'

if (!(cor == 'Azul')) {
    console.log('Não temos essa cor!')
}